# Copy to .env and adjust if needed. Compose sets DATABASE_URL on the app service by default.

# Use postgresql+psycopg for the FastAPI app (SQLAlchemy). Plain postgresql:// is normalized at runtime too.
DATABASE_URL=postgresql+psycopg://cafe:cafe@localhost:5432/cafe
